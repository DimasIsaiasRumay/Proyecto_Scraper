playwright==1.62.0
# A partir de la unificación con odoo-integration (main.py importa odoo_sync
# directamente para poder ejecutar scraping + sync con --sync en un solo proceso),
# las dependencias del módulo odoo-integration también son necesarias aquí. Se
# referencian en vez de copiarlas a mano, para que no se desincronicen si algún
# día cambian en odoo-integration/requirements.txt.
-r ../odoo-integration/requirements.txt
